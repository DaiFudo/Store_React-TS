import IUser from "../store/interface/interfaceUsers";
/* import usersData from "../database/users.json";

import Store from "../store/storeAccount";

export const handleAccount = async () => {
  const handleAccount: IUser = JSON.parse(JSON.stringify(usersData));
  Store.handleAccount(UsersData);
}; */
