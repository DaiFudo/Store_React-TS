import ICardInfo from "../../store/interface/interfaceCard";
/*const buyingOrDeleteItemInStore = (item: ICardInfo, lenghtPrompt: number) => {
  const items: any = { ...localStorage };
  console.log("items:", items, "item.name:", item.name);
  if (item.name == items) {
    console.log("yes");
  }
};
export default buyingOrDeleteItemInStore;
 */
